[phases.setup]
nixPkgs = ["nodejs_22", "jdk21", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "java -version",
  "pnpm run build:railway"
]

[start]
cmd = "java -version && node artifacts/api-server/dist/index.mjs"
