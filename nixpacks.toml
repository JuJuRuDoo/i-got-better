[phases.setup]
nixPkgs = ["nodejs_22", "jdk21"]

[phases.install]
cmds = [
  "npm install -g pnpm@10.26.1",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "java -version",
  "pnpm run build:railway"
]

[start]
cmd = "java -version && node artifacts/api-server/dist/index.mjs"
